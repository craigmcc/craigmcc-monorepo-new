# Button

`Button` provides a styled action trigger. For full prop details, this page reads from `content/meta/button.json`.

## Usage

```tsx
import { Button } from "@repo/daisy-ui/Button";

<Button color="primary">Save</Button>
```

