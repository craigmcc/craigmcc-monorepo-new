# Input

`Input` is a controlled text field with label/description/error slots.

## Usage

```tsx
import { Input } from "@repo/daisy-ui/Input";

<Input label="Email" name="email" value={email} handleChange={setEmail} />
```

